services:
  backend:
    build: ./backend
    ports:
      - 9000:9000

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - 8080:8080
    environment:
      BACKEND_DNS: "backend"